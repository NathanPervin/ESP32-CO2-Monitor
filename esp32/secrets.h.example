#define NUMBER_WIFI_OPTIONS 2

const char* WIFI_SSIDS[NUMBER_WIFI_OPTIONS] = {
    "example-wifi-ssid-1",
    "example-wifi-ssid-2"
};

const char* WIFI_PASSWORDS[NUMBER_WIFI_OPTIONS] = {
    "example-password-1",
    "example-password-2"
};
const char* SERVER_URL = "https://www.example.com/page-name";
const char* API_TOKEN = "your-api-token";

// Defaults used when the building/room fields are left blank on the start screen.
// Set these to your usual room so you can just press Start without re-entering them.
const char* DEFAULT_BUILDING = "debug";
const char* DEFAULT_ROOM = "0";